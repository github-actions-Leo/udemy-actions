name: Events
on:
    push:
        branches: [master]
    pull_request:
        branches: [master, feature/*]
        types: [opened, assigned]
    issues:
        types: [opened]
jobs:
    checkout-test:
        runs-on: ubuntu-latest
        steps:
            - name: Step
              run: |
                echo $GITHUB_SHA
                echo $GITHUB_REF
            - uses: actions/checkout@v4
